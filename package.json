{
  "name": "drag-relative-path",
  "main": "./lib/drag-relative-path",
  "version": "1.0.2",
  "description": "Drag files from the tree-view (or externally) to create HTML tags with automatic relative paths",
  "keywords": [
    "drag and drop",
    "relative path",
    "html tags"
  ],
  "repository": "https://github.com/Robbatron/drag-relative-path.git",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "relative": "^3.0.2"
  }
}
